#' Function to generate dissimilarity cube
#'
#' All functions developed by Stellenbosch BioMath for the b-cubed project
#'
#' @param arg1 Explain parameters here
#' @param ... Explain parameters here
#'
#' @return Explain what outputs are returned
#' @export
#'
#' @examples dissim(1)
#' @examples dissim('Also works for character strings')

dissim = function(arg1,...){
  # dataGEN option 1 Returns
  # metrics of spatial and temporal turnovers
  # there needs to be practical solution to visualise higher-order zeta (order 1 richness [related Hill numbers], order 2 beta with different metrics)
  # Return, visualisation options [maps GeoTIFFs], site by site matrices, per temporal unit. Temporal hotspots, statistics tables, PCA/NMDS and CCA visualisation.
  # functional/phylogenetic dissimilarity?
  # send Return to Zenodo
  return(arg1)
}
