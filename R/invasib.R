#' Function to generate invasibility cube
#'
#' All functions developed by Stellenbosch BioMath for the b-cubed project
#'
#' @param arg1 Explain parameters here
#' @param ... Explain parameters here
#'
#' @return Explain what outputs are returned
#' @export
#'
#' @examples invasib(1)
#' @examples invasib('Also works for character strings')

invasib = function(arg1,...){
  # dataGEN option 2 Returns
  # calculate trait distance matrix
  # specify SD of interaction kernel (could be assessed based on trait distance histogram or from 0 to max distance)
  # specify trait axes (could be PCA axes), incl. phylo?
  # Community trait profile, invasibility figure, trait specific invasiveness(function)
  # Return, invasive(trait) function, visual
  # send Return to Zenodo
  return(arg1)
}
